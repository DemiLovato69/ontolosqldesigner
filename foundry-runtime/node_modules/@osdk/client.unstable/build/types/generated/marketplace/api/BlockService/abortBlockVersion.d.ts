import { type ConjureContext } from "conjure-lite";
import type { BlockVersionId as _api_BlockVersionId } from "../__components.js";
/**
* Something went wrong when trying to make this block/provide block data, so abandon.
*
* User should have the "marketplace:create-block" permission on the MarketplaceRid. This operation
* is typically granted to users with the Editor role.
*/
export declare function abortBlockVersion(ctx: ConjureContext, blockVersionId: _api_BlockVersionId): Promise<void>;
