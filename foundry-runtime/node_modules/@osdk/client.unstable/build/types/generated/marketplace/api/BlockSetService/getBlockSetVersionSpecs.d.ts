import { type ConjureContext } from "conjure-lite";
import type { BlockSetVersionId as _api_BlockSetVersionId, GetBlockSetVersionSpecsResponse as _api_GetBlockSetVersionSpecsResponse } from "../__components.js";
/**
* Get the specs in block set version that were last processing or are currently being processed. Only includes
* specs that were explicitly put with `updateBlockSetVersionSpecs`.
* Excludes any dependencies implicitly added.
*
* Supports both pending and finalized block sets.
*
* Throws `BlockSetVersionNotFound` if the block set does not exist or if the user does not have
* `marketplace:read-local-marketplace` on the store it is in.
*/
export declare function getBlockSetVersionSpecs(ctx: ConjureContext, blockSetVersionId: _api_BlockSetVersionId): Promise<_api_GetBlockSetVersionSpecsResponse>;
