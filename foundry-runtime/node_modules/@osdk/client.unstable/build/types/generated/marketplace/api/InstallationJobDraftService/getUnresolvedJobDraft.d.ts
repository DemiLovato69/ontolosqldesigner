import { type ConjureContext } from "conjure-lite";
import type { BlockSetInstallationJobRid as _api_BlockSetInstallationJobRid, GetUnresolvedJobDraftResponse as _api_GetUnresolvedJobDraftResponse } from "../__components.js";
/**
* Gets the unresolved installation job draft that was requested by the user. The unresolved state of a draft
* is always updated synchronously.
*
* Requires the `marketplace:view-installation-job-draft` operation on the given `jobRid`. This is currently
* inherited from the installation RIDs that the draft was created with.
*/
export declare function getUnresolvedJobDraft(ctx: ConjureContext, jobRid: _api_BlockSetInstallationJobRid): Promise<_api_GetUnresolvedJobDraftResponse>;
