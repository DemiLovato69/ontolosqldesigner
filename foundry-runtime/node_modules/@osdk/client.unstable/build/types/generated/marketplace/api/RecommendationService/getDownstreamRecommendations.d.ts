import { type ConjureContext } from "conjure-lite";
import type { BlockSetVersionId as _api_BlockSetVersionId, GetDownstreamRecommendationsResponse as _api_GetDownstreamRecommendationsResponse, MarketplaceRid as _api_MarketplaceRid } from "../__components.js";
/**
* Fetch downstream recommendations across all stores for products whose latest versions state this block set
* version fulfills its inputs, i.e. the recommendation version compatibility range is satisfied by the version
* associated with this `BlockSetVersionId`.
*
* The `BlockSetVersionId` can reference a pending, complete or imported block set. For a pending version, we use
* the version calculated in the latest update to the pending block set to check for compatibility.
*
* NOTE: This endpoint is potentially computationally expensive, so callers should try to minimize its usage.
*
* Throws `BlockSetVersionNotFound` if the `BlockSetVersionId` does not exist.
* Throws `GetBlockSetInMarketplacePermissionDenied` if the user does not have
* `marketplace:read-local-marketplace` on the provided `marketplaceRid`.
*/
export declare function getDownstreamRecommendations(ctx: ConjureContext, marketplaceRid: _api_MarketplaceRid, blockSetVersionId: _api_BlockSetVersionId): Promise<_api_GetDownstreamRecommendationsResponse>;
