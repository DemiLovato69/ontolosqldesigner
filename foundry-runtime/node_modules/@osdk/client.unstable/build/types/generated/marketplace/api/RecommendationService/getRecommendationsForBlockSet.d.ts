import { type ConjureContext } from "conjure-lite";
import type { BlockSetVersionId as _api_BlockSetVersionId, GetRecommendationsResponse as _api_GetRecommendationsResponse, MarketplaceRid as _api_MarketplaceRid } from "../__components.js";
/**
* Fetch any recommendations for inputs to the finalized block set that were computed during packaging.
* If no recommendations were found for the BlockSetVersionId, we return an empty set.
* Filters to recommendations where the upstream BlockSetId is present on the stack, which may not be the case
* for recommendations that were shipped cross-stack. Does not guarantee that the upstream BlockSetId has a
* compatible version on this stack.
*
* `V2 compatible`: Compatible with block sets created with the V2 endpoint. The recommendations will always
* be finalized alongside the block set version.
*
* Throws `GetBlockSetInMarketplacePermissionDenied` if the user does not have
* `marketplace:read-local-marketplace` on the provided `marketplaceRid`.
* Throws `BlockSetVersionNotFound` if the `BlockSetVersionId` does not exist.
*/
export declare function getRecommendationsForBlockSet(ctx: ConjureContext, marketplaceRid: _api_MarketplaceRid, blockSetVersionId: _api_BlockSetVersionId): Promise<_api_GetRecommendationsResponse>;
