export { c as createClientWithTransaction } from '../createClient-D9uI6YEi.cjs';
import { C as Client } from '../Client-ByJ7d2BQ.cjs';
import '@osdk/api';
import '@osdk/foundry.ontologies';
import '@osdk/shared.client';
import '@osdk/api/unstable';
import '@osdk/shared.client2';

/**
 * Experimental helper method to create a new client instantiated with a transaction id
 * Will extract the transactionId, URL, Ontology RID, and token from the original client
 * unless overridden.
 *
 * @param writeableClient A client initiated with a transaction id, such as a WriteableClient
 * @param options A set of options to override from the provided client.
 * @returns Client instantiated on a transaction
 */
declare function createClientFromWriteableClient(writeableClient: Client, options?: {
    transactionId?: string;
    baseUrl?: string;
    ontologyRid?: string | Promise<string>;
    tokenProvider?: () => Promise<string>;
}): Client;

export { createClientFromWriteableClient };
