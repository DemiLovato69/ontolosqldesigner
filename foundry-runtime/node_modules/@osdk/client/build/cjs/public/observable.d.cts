import { a as QueryReturnType, A as ActionSignatureFromDef, C as Client } from '../Client-ByJ7d2BQ.cjs';
export { Q as QueryParameterType } from '../Client-ByJ7d2BQ.cjs';
import { ObjectTypeDefinition, InterfaceDefinition, PropertyKeys, ObjectOrInterfaceDefinition, WirePropertyTypes, WhereClause, DerivedProperty, ObjectSet, LinkNames, MediaMetadata, CompileTimeMetadata, PrimaryKeyType, Osdk, SimplePropertyDef, AggregateOpts, AggregationsResults, QueryDefinition, ActionDefinition, ActionEditResponse, ActionValidationResponse, MediaPropertyLocation } from '@osdk/api';
import '@osdk/shared.client';
import '@osdk/api/unstable';
import '@osdk/shared.client2';

type Canonical<T> = T & {
    __canonical: true;
};

/**
 * Represents the current state of an observation.
 *
 * - init: Initial state before any data fetch attempt
 * - loading: Request is in progress
 * - loaded: Data successfully retrieved
 * - error: Request failed
 */
type Status = "init" | "loading" | "loaded" | "error";
/**
 * Standard observer pattern interface for reactive data flow.
 *
 * @template T Type of data being observed
 *
 * Callbacks:
 * - next: Called with updated values when data changes
 * - error: Called when an error occurs
 * - complete: Called when observation completes (rarely used)
 */
interface Observer<T> {
    next: (value: T) => void;
    error: (err: any) => void;
    complete: () => void;
}
/**
 * Common options shared by all observation methods.
 *
 * @property dedupeInterval - Time in milliseconds to deduplicate identical emissions
 *   Used to prevent rapid UI updates when multiple changes occur
 */
interface CommonObserveOptions extends ObserveOptions {
    dedupeInterval?: number;
}
/**
 * Options for object observation.
 *
 * @property mode - Data fetch strategy:
 *   - offline: Use only cached data, no network request
 *   - force: Always fetch from server, ignore cache
 *   - undefined (default): Use cache if available, fetch if missing
 */
interface ObserveOptions {
    mode?: "offline" | "force";
}
/**
 * Specifies sort order for collection queries.
 *
 * @template Q Object or interface type whose properties can be sorted
 *
 * Values:
 * - "asc": Sort in ascending order
 * - "desc": Sort in descending order
 * - undefined: Don't sort by this property
 *
 * Example: `{ name: "asc", created: "desc" }`
 */
type OrderBy$1<Q extends ObjectTypeDefinition | InterfaceDefinition> = {
    [K in PropertyKeys<Q>]?: "asc" | "desc" | undefined;
};
/**
 * Defines how data should be handled during invalidation.
 *
 * Values:
 * - "in-place": Update data without resetting the collection
 * - "wait": Wait for the server response before updating
 * - "reset": Clear existing data and refetch entirely
 */
type InvalidationMode = "in-place" | "wait" | "reset";

interface ObserveObjectSetOptions<Q extends ObjectOrInterfaceDefinition, RDPs extends Record<string, WirePropertyTypes | undefined | Array<WirePropertyTypes>> = {}> extends CommonObserveOptions {
    where?: WhereClause<Q>;
    withProperties?: {
        [K in keyof RDPs]: DerivedProperty.Creator<Q, RDPs[K]>;
    };
    union?: ObjectSet<Q>[];
    intersect?: ObjectSet<Q>[];
    subtract?: ObjectSet<Q>[];
    /**
     * Traverse to linked objects. Cannot be combined with `streamUpdates`.
     * The server does not support websocket subscriptions for link-traversal
     * queries.
     */
    pivotTo?: LinkNames<Q>;
    pageSize?: number;
    orderBy?: {
        [K in PropertyKeys<Q>]?: "asc" | "desc";
    };
    /**
     * Restrict which properties are returned for each object.
     * When provided, only the specified properties will be fetched,
     * reducing payload sizes for list views.
     */
    select?: readonly PropertyKeys<Q>[];
    /**
     * Automatically fetch additional pages on initial load.
     *
     * - `true`: Fetch all available pages automatically
     * - `number`: Fetch pages until at least this many items are loaded
     * - `undefined` (default): Only fetch the first page, user must call fetchMore()
     */
    autoFetchMore?: boolean | number;
    /**
     * Enable streaming updates via websocket subscription.
     * When true, the object set will automatically update when matching objects are
     * added, updated, or removed.
     *
     * Cannot be combined with `pivotTo`. The server does not support
     * websocket subscriptions for link-traversal queries.
     *
     * Cannot be combined with `withProperties` (or a `baseObjectSet` that already
     * has derived properties applied). The server does not support websocket
     * subscriptions for object sets that include derived properties; in that
     * case `streamUpdates` is ignored and a warning is logged in development.
     *
     * @default false
     */
    streamUpdates?: boolean;
    /**
     * When true, loads per-property security metadata (marking requirements)
     * alongside each object. The returned objects will have `$propertySecurities`
     * populated with conjunctive/disjunctive marking requirements per property.
     */
    $loadPropertySecurityMetadata?: boolean;
}

interface MediaMetadataPayload {
    metadata: MediaMetadata | undefined;
    status: Status;
    lastUpdated: number;
    isOptimistic: boolean;
}
interface MediaMetadataObserveOptions {
    mode?: "offline" | "force";
    dedupeInterval?: number;
    preview?: boolean;
}

interface Unsubscribable$1 {
    unsubscribe: () => void;
}

declare namespace ObserveLinks {
    interface Options<Q extends ObjectTypeDefinition | InterfaceDefinition, L extends keyof CompileTimeMetadata<Q>["links"] & string> extends CommonObserveOptions, ObserveOptions {
        srcType: Pick<Q, "type" | "apiName">;
        sourceUnderlyingObjectType: string;
        pk: PrimaryKeyType<Q>;
        linkName: L;
        where?: WhereClause<CompileTimeMetadata<Q>["links"][L]["targetType"]>;
        select?: readonly string[];
        pageSize?: number;
        orderBy?: OrderBy$1<CompileTimeMetadata<Q>["links"][L]["targetType"]>;
        invalidationMode?: InvalidationMode;
        expectedLength?: number;
        /**
         * When true, includes all properties of the underlying concrete object type
         * when the link target is an interface. Has no effect for non-interface
         * targets.
         */
        $includeAllBaseObjectProperties?: boolean;
        /**
         * When traversing to linked objects via an interface link target, return
         * the full concrete object type instances instead of interface views.
         * Has no effect when the link target is already an object type.
         *
         * @default false
         */
        resolveToObjectType?: boolean;
    }
    interface CallbackArgs<T extends ObjectTypeDefinition | InterfaceDefinition> {
        resolvedList: Osdk.Instance<T, "$allBaseProperties">[] | undefined;
        linkedObjectsBySourcePrimaryKey: ReadonlyMap<string | number, ReadonlyArray<Osdk.Instance<T, "$allBaseProperties">>>;
        isOptimistic: boolean;
        lastUpdated: number;
        fetchMore: () => Promise<void>;
        hasMore: boolean;
        status: Status;
    }
}
interface ObserveLinks {
    observeLinks<T extends ObjectTypeDefinition | InterfaceDefinition, L extends keyof CompileTimeMetadata<T>["links"] & string>(objects: Osdk.Instance<T> | ReadonlyArray<Osdk.Instance<T>>, linkName: L, options: Omit<ObserveLinks.Options<T, L>, "srcType" | "pk" | "sourceUnderlyingObjectType">, subFn: Observer<ObserveLinks.CallbackArgs<CompileTimeMetadata<T>["links"][L]["targetType"]>>): Unsubscribable$1;
}

interface OptimisticBuilder {
    updateObject: <T extends ObjectTypeDefinition>(value: Osdk.Instance<T>) => this;
    createObject: <T extends ObjectTypeDefinition>(type: T, primaryKey: PrimaryKeyType<T>, properties: CompileTimeMetadata<T>["props"]) => this;
    deleteObject: <T extends ObjectTypeDefinition>(value: Osdk.Instance<T>) => this;
}

interface CacheSnapshot {
    entries: CacheEntry[];
    stats: {
        totalEntries: number;
        totalSize: number;
        totalHits?: number;
    };
}
interface CacheEntryMetadata {
    timestamp: number;
    status: "init" | "loading" | "loaded" | "error";
    hitCount?: number;
    size: number;
    isOptimistic?: boolean;
}
interface CacheEntryBase {
    key: string;
    objectType: string;
    metadata: CacheEntryMetadata;
    data?: unknown;
}
type CacheEntry = CacheEntryBase & {
    type: "object";
} | CacheEntryBase & {
    type: "list";
    where?: unknown;
    orderBy?: unknown;
    pageSize?: number;
} | CacheEntryBase & {
    type: "link";
    linkName?: string;
} | CacheEntryBase & {
    type: "objectSet";
};
interface ObserveObjectOptions<T extends ObjectOrInterfaceDefinition> extends ObserveOptions {
    apiName: T["apiName"] | T;
    pk: PrimaryKeyType<T>;
    select?: readonly PropertyKeys<T>[];
    $loadPropertySecurityMetadata?: boolean;
    /**
     * When true, includes all properties of the underlying concrete object type
     * for interface queries. Has no effect for non-interface queries.
     */
    $includeAllBaseObjectProperties?: boolean;
}
type OrderBy<Q extends ObjectOrInterfaceDefinition> = {
    [K in PropertyKeys<Q>]?: "asc" | "desc" | undefined;
};
interface ObserveListOptions<Q extends ObjectOrInterfaceDefinition, RDPs extends Record<string, SimplePropertyDef> = {}> extends CommonObserveOptions, ObserveOptions {
    type: Pick<Q, "apiName" | "type">;
    where?: WhereClause<Q, RDPs>;
    pageSize?: number;
    orderBy?: OrderBy<Q>;
    invalidationMode?: InvalidationMode;
    expectedLength?: number;
    /**
     * Enable streaming updates via websocket subscription.
     *
     * Cannot be combined with `pivotTo`. The server does not support
     * websocket subscriptions for link-traversal queries.
     *
     * Cannot be combined with `withProperties`. The server does not support
     * websocket subscriptions for object sets that include derived properties;
     * in that case `streamUpdates` is ignored and a warning is logged in
     * development.
     */
    streamUpdates?: boolean;
    withProperties?: DerivedProperty.Clause<Q>;
    /**
     * Fetch objects by their Resource Identifiers (RIDs).
     * When provided, starts with a static objectset containing these RIDs.
     * Can be combined with `where` to filter the RID set, and with `orderBy` to sort results.
     *
     * @example
     * ```ts
     * // Fetch specific objects by RID
     * observeList({ type: Employee, rids: ['ri.foo.123', 'ri.foo.456'] }, observer);
     * ```
     *
     * @example
     * ```ts
     * // Fetch specific objects by RID, filtered by status
     * observeList({
     *   type: Employee,
     *   rids: ['ri.foo.123', 'ri.foo.456', 'ri.foo.789'],
     *   where: { status: 'active' },
     * }, observer);
     * ```
     */
    rids?: readonly string[];
    /**
     * Restrict which properties are returned for each object.
     * When provided, only the specified properties will be fetched,
     * reducing payload sizes for list views.
     */
    select?: readonly PropertyKeys<Q>[];
    /**
     * When true, loads per-property security metadata (marking requirements)
     * alongside each object. The returned objects will have `$propertySecurities`
     * populated with conjunctive/disjunctive marking requirements per property.
     */
    $loadPropertySecurityMetadata?: boolean;
    /**
     * When true, includes all properties of the underlying concrete object type
     * for interface queries. Has no effect for non-interface queries.
     */
    $includeAllBaseObjectProperties?: boolean;
    /**
     * Automatically fetch additional pages on initial load.
     *
     * - `true`: Fetch all available pages automatically
     * - `number`: Fetch pages until at least this many items are loaded
     * - `undefined` (default): Only fetch the first page, user must call fetchMore()
     *
     * @example
     * ```ts
     * // Fetch all todos at once
     * observeList({ type: Todo, autoFetchMore: true }, observer);
     * ```
     *
     * @example
     * ```ts
     * // Fetch at least 100 todos
     * observeList({ type: Todo, autoFetchMore: 100, pageSize: 25 }, observer);
     * ```
     */
    autoFetchMore?: boolean | number;
    intersectWith?: Array<{
        where: WhereClause<Q, RDPs>;
    }>;
    /**
     * Traverse to linked objects. Cannot be combined with `streamUpdates`.
     * The server does not support websocket subscriptions for link-traversal
     * queries.
     */
    pivotTo?: string;
    /**
     * When loading objects via an interface type, return the full concrete
     * object type instances instead of interface views.
     *
     * By default, interface queries return objects narrowed to interface
     * properties only. With `resolveToObjectType: true`, returned objects
     * include all properties from the implementing object type (e.g. `$title`,
     * custom properties not on the interface).
     *
     * Only has an effect when `type` is an interface.
     *
     * @default false
     */
    resolveToObjectType?: boolean;
}
interface ObserveObjectCallbackArgs<T extends ObjectOrInterfaceDefinition> {
    object: Osdk.Instance<T, "$allBaseProperties"> | undefined;
    isOptimistic: boolean;
    status: Status;
    lastUpdated: number;
}
interface ObserveObjectsCallbackArgs<T extends ObjectOrInterfaceDefinition, RDPs extends Record<string, WirePropertyTypes | undefined | Array<WirePropertyTypes>> = {}> {
    resolvedList: Array<Osdk.Instance<T, "$allBaseProperties", PropertyKeys<T>, RDPs>> | undefined;
    isOptimistic: boolean;
    lastUpdated: number;
    fetchMore: () => Promise<void>;
    hasMore: boolean;
    status: Status;
    totalCount?: string;
    objectSet: ObjectSet<T, RDPs>;
}
interface ObserveObjectSetArgs<T extends ObjectOrInterfaceDefinition, RDPs extends Record<string, WirePropertyTypes | undefined | Array<WirePropertyTypes>> = {}> {
    resolvedList: Array<Osdk.Instance<T, "$allBaseProperties", PropertyKeys<T>, RDPs>> | undefined;
    isOptimistic: boolean;
    lastUpdated: number;
    fetchMore: () => Promise<void>;
    hasMore: boolean;
    status: Status;
    objectSet: ObjectSet<T, RDPs>;
    totalCount?: string;
}
interface ObserveAggregationBaseOptions<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>, RDPs extends Record<string, SimplePropertyDef> = {}> extends CommonObserveOptions, ObserveOptions {
    type: T;
    where?: WhereClause<T, RDPs>;
    withProperties?: DerivedProperty.Clause<T>;
    intersectWith?: Array<{
        where: WhereClause<T, RDPs>;
    }>;
    aggregate: A;
}
/**
 * Options for observeAggregation without an ObjectSet (synchronous).
 */
interface ObserveAggregationOptions<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>, RDPs extends Record<string, SimplePropertyDef> = {}> extends ObserveAggregationBaseOptions<T, A, RDPs> {
    objectSet?: undefined;
}
/**
 * Options for observeAggregation with an ObjectSet (asynchronous).
 *
 * When objectSet is provided, the aggregation is performed on that ObjectSet
 * instead of the base type, enabling aggregation on pivoted or filtered sets.
 */
interface ObserveAggregationOptionsWithObjectSet<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>, RDPs extends Record<string, SimplePropertyDef> = {}> extends ObserveAggregationBaseOptions<T, A, RDPs> {
    objectSet: ObjectSet<T>;
}
interface ObserveAggregationArgs<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>> {
    result: AggregationsResults<T, A> | undefined;
    status: Status;
    lastUpdated: number;
    error?: Error;
}
interface ObserveFunctionOptions extends CommonObserveOptions {
    /**
     * Object types this function depends on.
     * When actions modify these types, the function will refetch.
     */
    dependsOn?: Array<ObjectTypeDefinition | string>;
    /**
     * Specific object instances or ObjectSets this function depends on.
     * When these objects change, the function will refetch.
     *
     * For ObjectSets, the object type is extracted asynchronously and added
     * to the dependency list. Changes to any object of that type will trigger
     * a refetch.
     */
    dependsOnObjects?: Array<Osdk.Instance<ObjectTypeDefinition> | ObjectSet<ObjectTypeDefinition>>;
}
interface ObserveFunctionCallbackArgs<Q extends QueryDefinition<unknown>> {
    result: QueryReturnType<CompileTimeMetadata<Q>["output"]> | undefined;
    status: Status;
    lastUpdated: number;
    error?: Error;
}
declare namespace ObservableClient {
    interface ApplyActionOptions {
        optimisticUpdate?: (ctx: OptimisticBuilder) => void;
    }
}
/**
 * Public interface for reactive data management with automatic updates.
 *
 * The ObservableClient provides a reactive data layer with:
 * - Real-time object and collection observation
 * - Automatic cache updates when data changes
 * - Optimistic updates for immediate UI feedback
 * - Pagination support for large collections
 * - Link traversal for relationship navigation
 */
interface ObservableClient extends ObserveLinks {
    /**
     * Observe a single object or interface instance with automatic updates when it changes.
     *
     * @param apiName - The object type or interface definition, or its API name
     * @param pk - The object's primary key
     * @param options - Observation options including deduplication interval
     * @param subFn - Observer that receives object state updates
     * @returns Subscription that can be unsubscribed to stop updates
     *
     * The observer will receive:
     * - Initial loading state if data not cached
     * - Loaded state with the object data
     * - Updates when the object changes
     * - Error state if fetch fails
     */
    observeObject<T extends ObjectOrInterfaceDefinition>(apiName: T["apiName"] | T, pk: PrimaryKeyType<T>, options: ObserveOptions & Omit<ObserveObjectOptions<T>, "apiName" | "pk">, subFn: Observer<ObserveObjectCallbackArgs<T>>): Unsubscribable;
    /**
     * Observe a filtered and sorted collection of objects.
     *
     * @param options - Filter, sort, and pagination options
     * @param subFn - Observer that receives collection state updates
     * @returns Subscription that can be unsubscribed to stop updates
     *
     * Supports:
     * - Filtering with where clauses
     * - Sorting with orderBy
     * - Pagination via fetchMore() in the payload
     * - Automatic updates when any matching object changes
     */
    observeList<T extends ObjectOrInterfaceDefinition, RDPs extends Record<string, SimplePropertyDef> = {}>(options: ObserveListOptions<T, RDPs>, subFn: Observer<ObserveObjectsCallbackArgs<T, RDPs>>): Unsubscribable;
    /**
     * Observe an ObjectSet with automatic updates when matching objects change.
     *
     * @param baseObjectSet - The base ObjectSet to observe
     * @param options - Options for transforming and observing the ObjectSet
     * @param subFn - Observer that receives ObjectSet state updates
     * @returns Subscription that can be unsubscribed to stop updates
     *
     * Supports all ObjectSet operations:
     * - Filtering with where clauses
     * - Derived properties with withProperties
     * - Set operations (union, intersect, subtract)
     * - Link traversal with pivotTo
     * - Sorting and pagination
     */
    observeObjectSet<T extends ObjectOrInterfaceDefinition, RDPs extends Record<string, WirePropertyTypes | undefined | Array<WirePropertyTypes>> = {}>(baseObjectSet: ObjectSet<T>, options: ObserveObjectSetOptions<T, RDPs>, subFn: Observer<ObserveObjectSetArgs<T, RDPs>>): Unsubscribable;
    /**
     * @deprecated Use the async overload with `objectSet` parameter instead.
     * Pass `objectSet: client(YourType)` to get the base object set.
     */
    observeAggregation<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>, RDPs extends Record<string, SimplePropertyDef> = {}>(options: ObserveAggregationOptions<T, A, RDPs>, subFn: Observer<ObserveAggregationArgs<T, A>>): Unsubscribable;
    /**
     * Observe an aggregation query on a custom ObjectSet with automatic updates.
     *
     * This overload accepts an ObjectSet parameter, enabling aggregation on pivoted,
     * filtered, or composed ObjectSets. Returns a Promise because invalidation type
     * computation is async (requires lookups for link targets).
     *
     * @param options - Aggregation configuration including objectSet, where, aggregate spec
     * @param subFn - Observer that receives aggregation result updates
     * @returns Promise resolving to subscription that can be unsubscribed
     *
     * @example
     * ```typescript
     * const sub = await observableClient.observeAggregation(
     *   {
     *     type: Office,
     *     objectSet: $(Employee).pivotTo("primaryOffice"),
     *     aggregate: { $select: { $count: "unordered" } }
     *   },
     *   observer
     * );
     * ```
     */
    observeAggregation<T extends ObjectOrInterfaceDefinition, A extends AggregateOpts<T>, RDPs extends Record<string, SimplePropertyDef> = {}>(options: ObserveAggregationOptionsWithObjectSet<T, A, RDPs>, subFn: Observer<ObserveAggregationArgs<T, A>>): Promise<Unsubscribable>;
    /**
     * Observe a function execution with automatic updates.
     *
     * @param queryDef - The QueryDefinition to execute
     * @param params - Parameters to pass to the function (undefined if no params)
     * @param options - Observation options including invalidation configuration
     * @param subFn - Observer that receives function result updates
     * @returns Subscription that can be unsubscribed to stop updates
     *
     * Supports:
     * - Automatic caching and deduplication
     * - Dependency-based invalidation (dependsOn object types)
     * - Instance-based invalidation (dependsOnObjects)
     * - Manual refetch via invalidateFunction()
     */
    observeFunction<Q extends QueryDefinition<unknown>>(queryDef: Q, params: Record<string, unknown> | undefined, options: ObserveFunctionOptions, subFn: Observer<ObserveFunctionCallbackArgs<Q>>): Unsubscribable;
    /**
     * Execute an action with optional optimistic updates.
     *
     * @param action - Action definition to execute
     * @param args - Arguments for the action
     * @param opts - Options including optimistic updates
     * @returns Promise that resolves when the action completes
     *
     * When providing optimistic updates:
     * - Changes appear immediately in the UI
     * - Server request still happens in background
     * - On success, server data replaces optimistic data
     * - On failure, optimistic changes automatically roll back
     */
    applyAction: <Q extends ActionDefinition<any>>(action: Q, args: Parameters<ActionSignatureFromDef<Q>["applyAction"]>[0] | Array<Parameters<ActionSignatureFromDef<Q>["applyAction"]>[0]>, opts?: ObservableClient.ApplyActionOptions) => Promise<ActionEditResponse>;
    /**
     * Validate action parameters without executing the action.
     *
     * @param action - Action definition to validate
     * @param args - Arguments to validate
     * @returns Promise with validation result
     *
     * Use this to:
     * - Pre-validate forms before submission
     * - Display warnings or errors in the UI
     * - Enable/disable action buttons based on validity
     */
    validateAction: <Q extends ActionDefinition<any>>(action: Q, args: Parameters<ActionSignatureFromDef<Q>["applyAction"]>[0]) => Promise<ActionValidationResponse>;
    /**
     * Invalidates the entire cache, forcing all queries to refetch.
     * Use sparingly as this can cause significant network traffic.
     */
    invalidateAll(): Promise<void>;
    /**
     * Invalidates specific objects in the cache.
     *
     * Interface instances invalidate their backing concrete object.
     * @param objects - Single object or array of objects to invalidate
     */
    invalidateObjects(objects: Osdk.Instance<ObjectOrInterfaceDefinition> | ReadonlyArray<Osdk.Instance<ObjectOrInterfaceDefinition>>): Promise<void>;
    /**
     * Invalidates all cached data for a specific object type.
     * This includes:
     * - All objects of the specified type
     * - All lists containing objects of this type
     * - All links where the source is of this type
     *
     * @param type - Object type definition or API name string
     * @returns Promise that resolves when invalidation is complete
     */
    invalidateObjectType<T extends ObjectTypeDefinition>(type: T | T["apiName"]): Promise<void>;
    /**
     * Invalidate function queries.
     * - If params undefined, invalidates ALL queries for this function
     * - If params provided, invalidates only the query with exact params match
     *
     * @param apiName - Function API name string or QueryDefinition
     * @param params - Optional params for exact match
     */
    invalidateFunction(apiName: string | QueryDefinition<unknown>, params?: Record<string, unknown>): Promise<void>;
    /**
     * Invalidate functions that depend on a specific object instance.
     *
     * @param apiName - Object type API name
     * @param primaryKey - Object primary key
     */
    invalidateFunctionsByObject(apiName: string, primaryKey: string | number): Promise<void>;
    getCacheSnapshot(): Promise<CacheSnapshot>;
    canonicalizeWhereClause: <T extends ObjectOrInterfaceDefinition, RDPs extends Record<string, SimplePropertyDef> = {}>(where: WhereClause<T, RDPs>) => Canonical<WhereClause<T, RDPs>>;
    canonicalizeOptions: <OS, T extends CanonicalizeOptionsInput<OS>>(options: T) => CanonicalizedOptions<T>;
    observeMediaMetadata(coords: MediaPropertyLocation, options: MediaMetadataObserveOptions, observer: Observer<MediaMetadataPayload>): Unsubscribable;
}
interface CanonicalizeOptionsInput<OS = ObjectSet<any, any>> {
    where?: object;
    withProperties?: object;
    orderBy?: Record<string, "asc" | "desc" | undefined>;
    aggregate?: object;
    intersectWith?: Array<{
        where: object;
    }>;
    union?: ReadonlyArray<OS>;
    intersect?: ReadonlyArray<OS>;
    subtract?: ReadonlyArray<OS>;
    $select?: ReadonlyArray<string>;
}
type CanonicalizedOptions<T extends CanonicalizeOptionsInput<any>> = {
    [K in keyof T]: T[K];
};
interface ObservableClientOptions {
    /**
     * Dev-only behaviors of the observable client. These options have no effect
     * in production builds, where the relevant code is stripped at build time.
     */
    devMode?: {
        /**
         * Artificial delay, in milliseconds, applied to single action results in
         * dev mode so optimistic updates are visible before the server response
         * lands. Only applied when an optimistic update is provided to the action.
         * Set to 0 to disable. Defaults to 1000.
         */
        actionDelayMs?: number;
        /**
         * Log level for the observable client in dev mode. The client's loggers
         * default to `"error"`, which hides the `debug`-level tracing the observable
         * layer emits; set this to `"debug"` (or lower) to surface that tracing.
         * Inherited by every child logger the observable client creates.
         */
        logLevel?: ObservableClientLogLevel;
        /**
         * Verbose cache-internals logging in dev mode, off by default. These logs
         * are written with `console` directly and are stripped from production
         * builds.
         */
        debug?: {
            /**
             * Logs cache-entry reference-count lifecycle: creation, live counts,
             * time-to-live countdown, cleanup, and finalization. Also shortens the
             * retention window so cleanup is observable sooner.
             */
            refCounts?: boolean;
            /**
             * Logs cache-key canonicalization lookups, reporting whether each looked
             * up key already existed in the trie.
             */
            cacheKeys?: boolean;
        };
    };
}
/**
 * Log levels accepted by {@link ObservableClientOptions.devMode}'s `logLevel`,
 * ordered from most to least verbose.
 */
type ObservableClientLogLevel = "trace" | "debug" | "info" | "warn" | "error" | "fatal";
declare function createObservableClient(client: Client, extraUserAgents?: () => string[], options?: ObservableClientOptions): ObservableClient;
interface Unsubscribable {
    unsubscribe: () => void;
}

export { ActionSignatureFromDef, type CacheEntry, type CacheSnapshot, type CanonicalizeOptionsInput, type CanonicalizedOptions, ObservableClient, type ObservableClientLogLevel, type ObservableClientOptions, type ObserveAggregationArgs, type ObserveFunctionCallbackArgs, type ObserveFunctionOptions, ObserveLinks, type ObserveObjectCallbackArgs, type ObserveObjectSetArgs, type ObserveObjectsCallbackArgs, type Observer, QueryReturnType, type Unsubscribable, createObservableClient };
