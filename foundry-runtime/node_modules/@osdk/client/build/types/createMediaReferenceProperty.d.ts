import type { Media, MediaFullMetadata, MediaMetadata, MediaReference } from "@osdk/api";
import type { MediaReference as CoreMediaReference } from "@osdk/foundry.core";
import type { MinimalClient } from "./MinimalClientContext.js";
export declare class MediaReferencePropertyImpl implements Media {
	#private;
	constructor(args: {
		client: MinimalClient
		objectApiName: string
		primaryKey: any
		propertyName: string
		mediaReference: CoreMediaReference
	});
	fetchContents(): Promise<Response>;
	fetchMetadata(): Promise<MediaMetadata>;
	/**
	* Fetches type-specific metadata via `MediaSets.metadata`. There is no Ontologies-backed
	* full-metadata endpoint, so this routes through the media set view using the embedded media
	* reference.
	*/
	fetchFullMetadata(): Promise<MediaFullMetadata>;
	getMediaReference(): MediaReference;
}
