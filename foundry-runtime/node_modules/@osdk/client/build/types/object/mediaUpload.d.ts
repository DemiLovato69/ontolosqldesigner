import type { Media, MediaReference, MediaUpload } from "@osdk/api";
export declare function isMedia(o: any): o is Media;
export declare function isMediaReference(o: any): o is MediaReference;
export declare function isMediaUpload(o: any): o is MediaUpload;
