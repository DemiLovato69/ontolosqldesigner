import type { Canonical } from "./Canonical.js";
export declare class SelectCanonicalizer {
	private cache;
	canonicalize(select: undefined): undefined;
	canonicalize(select: readonly string[]): Canonical<readonly string[]>;
	canonicalize(select: readonly string[] | undefined): Canonical<readonly string[]> | undefined;
}
