import type { Attachment, Media, MediaMetadata } from "@osdk/api";
import type { Observer } from "../../ObservableClient/common.js";
import type { MediaPropertyLocation } from "../../ObservableClient/MediaTypes.js";
import { AbstractHelper } from "../AbstractHelper.js";
import type { UnsubscribableWrapper } from "../UnsubscribableWrapper.js";
import type { MediaMetadataObserveOptions, MediaMetadataPayload } from "./MediaMetadataQuery.js";
import { MediaMetadataQuery } from "./MediaMetadataQuery.js";
export declare class MediaHelper extends AbstractHelper<MediaMetadataQuery, MediaMetadataObserveOptions> {
	private blobManager;
	getCacheKey(mediaOrLocation: Media | Attachment | MediaPropertyLocation): string;
	private getTypedCacheKey;
	getQuery(options: MediaMetadataObserveOptions & {
		coords: MediaPropertyLocation
	}): MediaMetadataQuery;
	observeMediaMetadata(coords: MediaPropertyLocation, options: MediaMetadataObserveOptions, observer: Observer<MediaMetadataPayload>): UnsubscribableWrapper;
	fetchMetadata(coords: MediaPropertyLocation, options?: {
		preview?: boolean
	}): Promise<MediaMetadata>;
	fetchContent(mediaOrLocation: Media | Attachment | MediaPropertyLocation, options?: {
		preview?: boolean
	}): Promise<Blob>;
	private resolveToCoords;
	getCachedContent(mediaOrLocation: Media | Attachment | MediaPropertyLocation, options?: {
		preview?: boolean
	}): Blob | undefined;
	getCachedMetadata(coords: MediaPropertyLocation): MediaMetadata | undefined;
	createBlobUrl(mediaOrLocation: Media | Attachment | MediaPropertyLocation, options?: {
		preview?: boolean
	}): string | undefined;
	releaseBlobUrl(mediaOrLocation: Media | Attachment | MediaPropertyLocation, options?: {
		preview?: boolean
	}): void;
	clearCache(mediaOrLocation: Media | Attachment | MediaPropertyLocation): void;
	clearAll(): void;
	dispose(): void;
}
